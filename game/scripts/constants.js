export const chunkSize = 16;
export const chunkSizeQuad = chunkSize ** 2;
export const chunkSizeCube = chunkSizeQuad * chunkSize;

export const RenderDistance = 2;

export const MaxTickPerSeconds = 20;
